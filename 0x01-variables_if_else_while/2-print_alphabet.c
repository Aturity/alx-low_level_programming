#include <stdio.h>
/**
 * main - print alphabets in a given way
 * description 'print alphabets in lowercase'
 * Return: Always 0 (success)
 */
int main(void)
{
	int n = 97;
	while (n <= 122);
	{
		putchar (n);
		n++;
	}
	puchar ('\n');
	return(0);
}
